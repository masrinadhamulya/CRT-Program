#include<stdio.h>
main()
{
    int n,r,sum;
    printf("\nEnter the Number ");
    scanf("%d",&n);
    sum=0;
    while(n>0)
    {
        r=n%10;
        sum=sum+r;
        n=n/10;
    }
    printf("%d is the sum of Digits",sum);
}