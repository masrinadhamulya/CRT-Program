#include<stdio.h>
main()
{
    int n,r,count;
    printf("\nEnter the Number ");
    scanf("%d",&n);
    count=0;
    while(n>0)
    {
        r=n%10;
        count++;
        n=n/10;
    }
    printf("%d is the No.of Digits ",count);
}