#include<stdio.h>
main()
{
    int a;
    printf("\n Enter the number ");
    scanf("%d",&a);
    if(a%5==0 && a%11==0)
     printf("\n %d is Divisible by 5 and 11",a);
    else if(a%5==0 && a%11!=0)
     printf("\n %d is Divisible by 5 not 11",a);
    else if(a%5!=0 && a%11==0)
     printf("\n %d is Divisible by 11 not 5",a);
    else
     printf("\n %d is not Divisible by 5 and 11",a);
}