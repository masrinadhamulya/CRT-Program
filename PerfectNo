#include<stdio.h>
main()
{
    int i,n,sum;
    sum=0;
    printf("\nEnter a Number ");
    scanf("%d",&n);
    for(i=1;i<n;i++)
    {
        if(n%i==0)
         sum=sum+i;   
    }
    if(sum==n)
     printf("\n%d is a Perfect No.",n);
    else
     printf("\n%d is not a Pertfect No. ",n);
}