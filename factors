#include<stdio.h>
main()
{
    int i,n;
    printf("\nEnter a Number ");
    scanf("%d",&n);
    printf("The Factors of %d are ",n);
    for(i=1;i<=n;i++)
    {
        if(n%i==0)
        printf(" %d ",i);
    }
}