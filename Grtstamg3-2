#include<stdio.h>
main()
{
    int a,b,c;
    printf("\n Enter the three values ");
    scanf("%d%d%d",&a,&b,&c);
    printf(a==b && b==c ? "All are euqal":a>b && a>c ? "A is Greater":b>c ? "B is Greater":"C is Greater");
}